tasks:
  - type: docker-build
    image: charon
    auto_push_on:
      - source: push
        has_tag: true
        tag_source: tag
      - source: push
        has_tag: true
        tag_source: latest
    context: .
    registries:
      - gitlab
  - type: pypi-build
    auto_push: true
    registries:
      - gitlab

